import css from "./index.module.scss";

export const IndexSelector = ({ mainIndex }) => {
  return (
    <>
      <div className={css.circle}></div>

      <div className={css.title}>{type.title}</div>
    </>
  );
};
